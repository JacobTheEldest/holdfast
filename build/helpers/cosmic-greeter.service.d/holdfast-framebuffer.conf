[Unit]
# Ensure the framebuffer console is unbound before the greeter starts.
# Without this, cosmic-comp runs in unprivileged mode and page flips fail
# with "Permission denied (os error 13)".
After=holdfast-unbind-framebuffer.service
